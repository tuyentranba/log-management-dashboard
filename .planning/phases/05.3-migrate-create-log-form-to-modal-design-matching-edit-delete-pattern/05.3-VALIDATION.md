---
phase: 05.3
slug: migrate-create-log-form-to-modal-design-matching-edit-delete-pattern
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-26
---

# Phase 05.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | jest 29.x + @testing-library/react 16.x |
| **Config file** | frontend/jest.config.ts |
| **Quick run command** | `cd frontend && npm test -- --testPathPattern="create" --maxWorkers=2` |
| **Full suite command** | `cd frontend && npm test` |
| **Estimated runtime** | ~3-5 seconds (quick), ~15-20 seconds (full) |

---

## Sampling Rate

- **After every task commit:** Run `cd frontend && npm test -- --testPathPattern="create" --maxWorkers=2`
- **After every plan wave:** Run `cd frontend && npm test`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 05.3-01-01 | 01 | 1 | None | unit | `grep -n "interface CreateFormProps" frontend/src/app/logs/_components/create-form.tsx` | ✅ | ⬜ pending |
| 05.3-01-02 | 01 | 1 | None | integration | `grep -n "CreateButton" frontend/src/app/logs/page.tsx && grep -n "CreateLogModal"` | ✅ | ⬜ pending |
| 05.3-01-03 | 01 | 1 | None | verify | `! test -d frontend/src/app/create && ! grep -q 'href="/create"' frontend/src/app/layout.tsx` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements.

Frontend test infrastructure already established in Phase 03-00:
- Jest 29.7.0 configured with Next.js 15 transforms
- React Testing Library 16.3.2 installed
- Test utilities with custom render and provider wrapper
- Global setup with router mocks

No Wave 0 work needed — tests can be written immediately alongside implementation.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Modal opens when clicking Create button | None (UX verification) | Visual state transition with URL change | 1. Navigate to /logs page<br>2. Click "+ Create Log" button in header<br>3. Verify modal opens with form<br>4. Verify URL shows ?create=true |
| Form submission closes modal and refreshes list | None (UX verification) | Integration of modal close + toast + list refresh | 1. Open create modal<br>2. Fill valid log data<br>3. Click Create Log button<br>4. Verify modal closes<br>5. Verify success toast appears<br>6. Verify new log appears in list |
| Modal closes on outside click/Escape | None (UX verification) | User interaction pattern | 1. Open create modal<br>2. Click backdrop or press Escape<br>3. Verify modal closes without warning<br>4. Verify URL ?create param cleared |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (N/A - no missing references)
- [x] No watch-mode flags
- [x] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter (pending execution)

**Approval:** pending 2026-03-26
