---
phase: 06-testing
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - Makefile
  - README.md
autonomous: true
requirements:
  - TEST-05
must_haves:
  truths:
    - "All tests (backend + frontend) runnable via single command 'make test'"
    - "Quick test runs skip slow performance tests via 'make test-quick'"
    - "Coverage reports generate for both backend and frontend via 'make coverage'"
    - "README documents how to run tests with clear examples"
  artifacts:
    - path: "Makefile"
      provides: "Test execution commands"
      contains: "test-quick"
    - path: "README.md"
      provides: "Testing documentation"
      contains: "## Testing"
  key_links:
    - from: "Makefile"
      to: "pytest"
      via: "test target"
      pattern: "docker-compose exec backend pytest"
    - from: "Makefile"
      to: "jest"
      via: "test target"
      pattern: "npm test"
---

<objective>
Add unified test execution commands to Makefile and document testing process in README

Purpose: Enable single-command test execution for all tests (backend + frontend) with coverage reporting and quick feedback mode, satisfying TEST-05 requirement

Output: Makefile targets (test, test-quick, coverage) functional, README.md Testing section complete with examples and philosophy
</objective>

<execution_context>
@/Users/tuyen.tranba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuyen.tranba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-testing/06-CONTEXT.md
@.planning/phases/06-testing/06-RESEARCH.md

# Files to modify
@Makefile
@README.md
</context>

<interfaces>
<!-- Existing Makefile patterns -->

From Makefile (existing targets):
```makefile
.PHONY: help start stop restart logs test seed migrate clean ps

test:
	@echo "Running backend tests..."
	docker-compose exec backend pytest tests/ -v
```

<!-- Expected test commands from plans 06-01 and 06-02 -->

Backend test commands:
```bash
# All tests with coverage
docker-compose exec backend pytest tests/ --cov=app --cov-report=term-missing --cov-report=html:htmlcov -v

# Quick tests (skip slow)
docker-compose exec backend pytest tests/ -v -m "not slow"
```

Frontend test commands:
```bash
# All tests with coverage
docker-compose exec frontend npm test -- --coverage --passWithNoTests

# Regular tests
docker-compose exec frontend npm test -- --passWithNoTests
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update Makefile with comprehensive test targets</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (see existing .PHONY declaration, test target, command patterns)
  </read_first>
  <action>
Update Makefile to replace existing test target and add new test-quick and coverage targets:

1. Update .PHONY line to include new targets:
```makefile
.PHONY: help start stop restart logs logs-backend logs-frontend logs-db test test-quick coverage seed migrate clean ps
```

2. Replace existing test target with comprehensive version:
```makefile
test:
	@echo "Running all tests (backend + frontend)..."
	@echo ""
	@echo "=== Backend Tests ==="
	docker-compose exec backend pytest tests/ -v
	@echo ""
	@echo "=== Frontend Tests ==="
	docker-compose exec frontend npm test -- --passWithNoTests
	@echo ""
	@echo "All tests complete!"
```

3. Add new test-quick target after test target:
```makefile
test-quick:
	@echo "Running quick tests (excluding slow performance tests)..."
	@echo ""
	@echo "=== Backend Quick Tests ==="
	docker-compose exec backend pytest tests/ -v -m "not slow"
	@echo ""
	@echo "=== Frontend Tests ==="
	docker-compose exec frontend npm test -- --passWithNoTests
	@echo ""
	@echo "Quick tests complete! (Performance tests skipped)"
```

4. Add new coverage target after test-quick:
```makefile
coverage:
	@echo "Running tests with coverage reports..."
	@echo ""
	@echo "=== Backend Coverage ==="
	docker-compose exec backend pytest tests/ --cov=app --cov-report=term-missing --cov-report=html:htmlcov
	@echo ""
	@echo "=== Frontend Coverage ==="
	docker-compose exec frontend npm test -- --coverage --passWithNoTests
	@echo ""
	@echo "Coverage reports generated:"
	@echo "  Backend:  backend/htmlcov/index.html"
	@echo "  Frontend: frontend/coverage/lcov-report/index.html"
	@echo ""
```

5. Update help target to include new test commands:
- Add line: "  make test-quick  - Run tests excluding slow performance tests (fast feedback)"
- Add line: "  make coverage    - Run all tests with coverage reports (HTML + terminal)"
- Update test line: "  make test        - Run all tests (backend + frontend)"

Why these specific commands:
- test: Runs both backend and frontend tests sequentially with clear output sections
- test-quick: Skips @pytest.mark.slow tests for fast feedback (<10s), useful during development
- coverage: Generates both terminal and HTML coverage reports with detailed metrics
- Clear echo statements provide progress feedback and report locations
  </action>
  <verify>
    <automated>
grep -q "test-quick:" Makefile && \
grep -q "coverage:" Makefile && \
grep -q "not slow" Makefile && \
grep -q "htmlcov/index.html" Makefile
    </automated>
  </verify>
  <acceptance_criteria>
    - Makefile .PHONY line includes test, test-quick, and coverage
    - Makefile test target runs both backend pytest and frontend npm test
    - Makefile test-quick target includes -m "not slow" flag for backend tests
    - Makefile coverage target includes --cov flags for backend and --coverage for frontend
    - Coverage target outputs HTML report locations (backend/htmlcov, frontend/coverage)
    - Help target documents all three test commands with descriptions
    - All targets use @echo for user feedback and clear output sections
  </acceptance_criteria>
  <done>Makefile updated with test, test-quick, and coverage targets enabling unified test execution for backend and frontend with single commands</done>
</task>

<task type="auto">
  <name>Task 2: Document testing process in README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (see existing structure, sections, formatting conventions)
  </read_first>
  <action>
Add comprehensive Testing section to README.md. Insert after existing sections (likely after "Running the Application" or "Development" section):

```markdown
## Testing

The project has comprehensive test coverage for both backend and frontend components, demonstrating production-ready quality with automated validation.

### Test Coverage

**Backend:**
- Unit tests for business logic (cursor utilities, schemas, configuration)
- Integration tests for all API endpoints (CRUD, list, analytics, export)
- Performance tests validating query execution times with 100k logs
- Target: 80%+ line coverage (measured by pytest-cov)

**Frontend:**
- Component tests for forms and modals (CreateForm, EditForm, LogDetailModal)
- API integration tests with mocked responses
- Accessibility validation with jest-axe
- Target: 80%+ line coverage (measured by Jest)

### Running Tests

**All tests (backend + frontend):**
```bash
make test
```

**Quick tests (skip slow performance tests):**
```bash
make test-quick
```
Useful for fast feedback during development. Skips tests marked with `@pytest.mark.slow` (~5-10 second run time).

**Tests with coverage reports:**
```bash
make coverage
```

Coverage reports are generated in:
- Backend: `backend/htmlcov/index.html`
- Frontend: `frontend/coverage/lcov-report/index.html`

Open these HTML files in a browser for detailed line-by-line coverage analysis.

### Running Tests Individually

**Backend only:**
```bash
docker-compose exec backend pytest tests/ -v
```

**Frontend only:**
```bash
docker-compose exec frontend npm test -- --passWithNoTests
```

**Specific test file:**
```bash
# Backend
docker-compose exec backend pytest tests/test_logs_crud.py -v

# Frontend
docker-compose exec frontend npm test -- --testPathPattern=create-form.test.tsx
```

### Test Organization

**Backend (`backend/tests/`):**
- `test_config.py` - Configuration loading and validation
- `test_cursor.py` - Cursor pagination utilities
- `test_schemas.py` - Pydantic schema validation
- `test_health.py` - Health endpoint integration tests
- `test_logs_crud.py` - CRUD operations (POST, GET, PUT, DELETE)
- `test_logs_list.py` - List endpoint with filtering and sorting
- `test_export.py` - CSV export streaming
- `test_analytics.py` - Analytics aggregations
- `test_logs_performance.py` - Performance validation with 100k logs (marked `@pytest.mark.slow`)

**Frontend (`frontend/__tests__/`):**
- `components/create-form.test.tsx` - Create log form validation and submission
- `components/edit-form.test.tsx` - Edit log form pre-population and updates
- `components/log-detail-modal.test.tsx` - Modal view/edit mode toggle
- `api/api-integration.test.tsx` - API client functions with mocked fetch

### Test Philosophy

- **Integration over unit:** Tests validate real behavior with real database and API calls (mocking only external dependencies)
- **Minimal mocking:** Backend tests use real PostgreSQL database for accurate validation
- **User-focused frontend tests:** Tests validate user interactions and accessibility (not implementation details)
- **Performance validation:** Hard thresholds ensure queries meet production requirements (<500ms pagination, <2s analytics)
- **Coverage as visibility:** 80% target demonstrates quality without over-testing trivial code

### Performance Test Thresholds

Performance tests (`test_logs_performance.py`) validate query execution times with 100k logs:

| Endpoint | Threshold | Validates |
|----------|-----------|-----------|
| Pagination (page 100+) | <500ms | Cursor pagination efficiency |
| Analytics aggregations | <2000ms | date_trunc and GROUP BY performance |
| CSV export with filters | <3000ms | Streaming response memory efficiency |
| Multi-filter queries | <500ms | Composite index utilization |

Tests fail if thresholds are exceeded, preventing performance regressions.
```

Placement guidelines:
- Insert after "Development" section if exists, or after "Running the Application"
- Use consistent markdown formatting with existing README
- Match heading level style (## for main sections)
- Use code blocks with bash syntax highlighting

Why this content:
- Satisfies DOC-02 requirement (document how to run tests)
- Documents coverage requirements per CONTEXT.md decisions
- Explains test philosophy demonstrating thoughtful approach
- Provides clear examples for all test commands
- Includes performance thresholds from ROADMAP.md success criteria
  </action>
  <verify>
    <automated>
grep -q "## Testing" README.md && \
grep -q "make test" README.md && \
grep -q "80%+ line coverage" README.md && \
grep -q "Performance Test Thresholds" README.md
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "## Testing" section header
    - Testing section documents coverage targets (80%+ for backend and frontend)
    - Testing section includes "make test", "make test-quick", "make coverage" commands with descriptions
    - Testing section explains coverage report locations (htmlcov, lcov-report)
    - Testing section lists test organization (all backend and frontend test files)
    - Testing section includes Test Philosophy subsection explaining approach
    - Testing section includes Performance Test Thresholds table with specific values
    - Code examples use bash syntax highlighting (```bash)
    - Content integrates naturally with existing README structure and formatting
  </acceptance_criteria>
  <done>README.md Testing section complete with comprehensive documentation of test commands, coverage targets, test organization, philosophy, and performance thresholds</done>
</task>

<task type="auto">
  <name>Task 3: Verify unified test execution</name>
  <files>None (verification task)</files>
  <read_first>
    - Makefile (confirm all test targets are present)
  </read_first>
  <action>
Execute all three Makefile test targets to verify functionality:

1. Run quick tests first (fast validation):
```bash
make test-quick
```
Expected: Backend tests pass (excluding slow), frontend tests pass, <30 seconds total

2. Run full test suite:
```bash
make test
```
Expected: All backend tests pass (including slow), all frontend tests pass, complete in <3 minutes

3. Run coverage reports:
```bash
make coverage
```
Expected:
- Backend coverage >= 80% shown in terminal
- Frontend coverage >= 80% shown in terminal
- backend/htmlcov/index.html exists and opens in browser
- frontend/coverage/lcov-report/index.html exists and opens in browser

4. Verify help target includes new commands:
```bash
make help
```
Expected: Output lists test, test-quick, coverage with descriptions

If any target fails:
- Check docker-compose services are running (make start)
- Verify pytest-cov installed in backend (docker-compose exec backend pip list | grep pytest-cov)
- Verify jest-axe installed in frontend (docker-compose exec frontend npm list jest-axe)
- Check for syntax errors in Makefile (tabs not spaces for command indentation)

Why verify all targets: Ensures TEST-05 requirement met (tests runnable via single command), confirms integration with plans 06-01 and 06-02
  </action>
  <verify>
    <automated>
make test-quick && \
make test && \
make coverage && \
test -f backend/htmlcov/index.html && \
test -f frontend/coverage/lcov-report/index.html
    </automated>
  </verify>
  <acceptance_criteria>
    - Command "make test-quick" executes successfully with exit code 0
    - Command "make test" executes successfully with exit code 0
    - Command "make coverage" executes successfully with exit code 0
    - Command "make help" output includes test, test-quick, and coverage descriptions
    - File backend/htmlcov/index.html exists after running make coverage
    - File frontend/coverage/lcov-report/index.html exists after running make coverage
    - Backend coverage report shows >= 80% line coverage
    - Frontend coverage report shows >= 80% line coverage
    - All tests pass with no failures in any target
  </acceptance_criteria>
  <done>All Makefile test targets verified functional, single-command test execution works for full suite and coverage reports, TEST-05 requirement satisfied</done>
</task>

</tasks>

<verification>
Verify complete test workflow from clean state:

```bash
# 1. Verify services running
docker-compose ps

# 2. Run quick tests (fast feedback)
make test-quick

# 3. Run full test suite
make test

# 4. Generate coverage reports
make coverage

# 5. Check coverage reports exist
ls -la backend/htmlcov/index.html
ls -la frontend/coverage/lcov-report/index.html

# 6. Verify help documentation
make help | grep -E "test|coverage"
```

Expected final state:
- All tests pass consistently
- Coverage reports show 80%+ for both backend and frontend
- HTML coverage reports accessible for detailed analysis
- README.md documents complete testing workflow
- Single command "make test" runs entire test suite
</verification>

<success_criteria>
1. Makefile has test, test-quick, and coverage targets
2. test target runs both backend and frontend tests sequentially
3. test-quick target skips slow performance tests for fast feedback
4. coverage target generates HTML and terminal reports for both backend and frontend
5. README.md has comprehensive Testing section with examples, philosophy, and thresholds
6. All three Makefile targets execute successfully
7. make help documents all test commands with clear descriptions
8. TEST-05 requirement satisfied (tests runnable via single command)
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing/06-03-SUMMARY.md`
</output>
