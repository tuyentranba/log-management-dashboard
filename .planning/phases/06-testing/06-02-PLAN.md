---
phase: 06-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/jest.config.js
  - frontend/__tests__/setup.ts
  - frontend/__tests__/components/create-form.test.tsx
  - frontend/__tests__/components/edit-form.test.tsx
  - frontend/__tests__/components/log-detail-modal.test.tsx
  - frontend/__tests__/api/api-integration.test.tsx
autonomous: true
requirements:
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "Frontend tests achieve 80%+ line coverage measured by Jest"
    - "Critical path components have tests covering user interactions and error cases"
    - "API client functions have tests with mocked responses"
    - "Forms validate user input and handle submission errors"
  artifacts:
    - path: "frontend/package.json"
      provides: "jest-axe dependency"
      contains: "jest-axe"
    - path: "frontend/jest.config.js"
      provides: "Coverage configuration"
      contains: "coverageThreshold"
    - path: "frontend/__tests__/components/create-form.test.tsx"
      provides: "CreateForm component tests"
      min_lines: 80
    - path: "frontend/__tests__/components/edit-form.test.tsx"
      provides: "EditForm component tests"
      min_lines: 80
    - path: "frontend/__tests__/components/log-detail-modal.test.tsx"
      provides: "LogDetailModal component tests"
      min_lines: 60
    - path: "frontend/__tests__/api/api-integration.test.tsx"
      provides: "API client integration tests"
      min_lines: 100
  key_links:
    - from: "frontend/__tests__/components/create-form.test.tsx"
      to: "jest.mock('@/lib/api')"
      via: "API mocking"
      pattern: "jest\\.mock\\('@/lib/api'\\)"
    - from: "frontend/jest.config.js"
      to: "jest-axe"
      via: "accessibility testing"
      pattern: "toHaveNoViolations"
---

<objective>
Add comprehensive frontend tests for critical paths (forms, modals, API integration) achieving 80%+ coverage with Jest and React Testing Library

Purpose: Validate user-facing functionality with tests covering form validation, submission, error handling, and API integration demonstrating production-ready frontend quality

Output: jest-axe configured, 4 new test files with 20+ tests covering CreateForm, EditForm, LogDetailModal, and API client functions, 80%+ line coverage
</objective>

<execution_context>
@/Users/tuyen.tranba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tuyen.tranba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-testing/06-CONTEXT.md
@.planning/phases/06-testing/06-RESEARCH.md

# Existing test infrastructure
@frontend/jest.config.js
@frontend/__tests__/setup.ts
@frontend/__tests__/utils/test-utils.tsx

# Components to test
@frontend/src/app/logs/_components/create-form.tsx
@frontend/src/app/logs/_components/edit-form.tsx
@frontend/src/app/logs/_components/log-detail-modal.tsx

# API client to test
@frontend/src/lib/api.ts
</context>

<interfaces>
<!-- Key types and patterns from existing test infrastructure and components -->

From frontend/__tests__/utils/test-utils.tsx:
```typescript
import { render } from '@testing-library/react'

function AllTheProviders({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}

export function customRender(ui: React.ReactElement, options?: any) {
  return render(ui, { wrapper: AllTheProviders, ...options })
}

export * from '@testing-library/react'
export { customRender as render }
```

From frontend/__tests__/setup.ts (existing mocks):
```typescript
import '@testing-library/jest-dom'

// Mock next/navigation
jest.mock('next/navigation', () => ({
  useRouter: jest.fn(() => ({
    push: jest.fn(),
    replace: jest.fn(),
  })),
  usePathname: jest.fn(() => '/logs'),
  useSearchParams: jest.fn(() => new URLSearchParams()),
}))
```

From frontend/src/lib/api.ts:
```typescript
export async function createLog(data: LogCreate): Promise<LogResponse>
export async function updateLog(id: number, data: LogCreate): Promise<LogResponse>
export async function deleteLog(id: number): Promise<void>
export async function fetchLogs(filters: LogFilters): Promise<LogListResponse>
```

From frontend/src/app/logs/_components/create-form.tsx:
```typescript
interface CreateFormProps {
  onSuccess?: () => void
  onCancel?: () => void
}

export function CreateForm({ onSuccess, onCancel }: CreateFormProps)
```

From frontend/src/app/logs/_components/edit-form.tsx:
```typescript
interface EditFormProps {
  log: LogResponse
  onSuccess: () => void
  onCancel: () => void
}

export function EditForm({ log, onSuccess, onCancel }: EditFormProps)
```

From frontend/src/app/logs/_components/log-detail-modal.tsx:
```typescript
interface LogDetailModalProps {
  log: LogResponse | null
  isOpen: boolean
  onClose: () => void
}

export function LogDetailModal({ log, isOpen, onClose }: LogDetailModalProps)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Configure jest-axe and coverage thresholds</name>
  <files>frontend/package.json, frontend/jest.config.js, frontend/__tests__/setup.ts</files>
  <read_first>
    - frontend/package.json (see devDependencies section)
    - frontend/jest.config.js (see existing customJestConfig object)
    - frontend/__tests__/setup.ts (see existing imports and mocks)
  </read_first>
  <action>
Add jest-axe to frontend/package.json devDependencies:
- Run: npm install --save-dev jest-axe
- Verify package.json includes "jest-axe": "^10.0.0" (or latest)

Update frontend/jest.config.js customJestConfig object:
- Add coverageThreshold property after collectCoverageFrom:
```javascript
coverageThreshold: {
  global: {
    lines: 80,
    statements: 80,
  },
},
```
- Add coverageReporters property:
```javascript
coverageReporters: [
  'text',              // Terminal summary
  'html',              // HTML report in coverage/ directory
  'lcov',              // LCOV for tooling integration
],
```
- Update collectCoverageFrom to exclude shadcn/ui components:
```javascript
collectCoverageFrom: [
  'src/**/*.{ts,tsx}',
  '!src/**/*.d.ts',
  '!src/**/*.stories.tsx',
  '!src/components/ui/**',  // Exclude shadcn/ui components
],
```

Update frontend/__tests__/setup.ts:
- Add jest-axe imports after @testing-library/jest-dom:
```typescript
import { toHaveNoViolations } from 'jest-axe'
expect.extend(toHaveNoViolations)
```

Why these specific values:
- 80% lines and statements: Matches backend coverage target, demonstrates quality
- Exclude src/components/ui/**: shadcn/ui components are third-party, no need to test
- jest-axe integration: Automated accessibility validation in all component tests
  </action>
  <verify>
    <automated>
npm list jest-axe && \
grep -q "coverageThreshold" frontend/jest.config.js && \
grep -q "toHaveNoViolations" frontend/__tests__/setup.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json devDependencies contains "jest-axe"
    - frontend/jest.config.js has coverageThreshold with lines: 80, statements: 80
    - frontend/jest.config.js has coverageReporters array with 'text', 'html', 'lcov'
    - frontend/jest.config.js collectCoverageFrom excludes '!src/components/ui/**'
    - frontend/__tests__/setup.ts imports toHaveNoViolations from jest-axe
    - frontend/__tests__/setup.ts calls expect.extend(toHaveNoViolations)
  </acceptance_criteria>
  <done>jest-axe installed and configured, coverage thresholds set to 80% for lines and statements, shadcn/ui components excluded from coverage measurement</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create form component tests (CreateForm, EditForm)</name>
  <files>frontend/__tests__/components/create-form.test.tsx, frontend/__tests__/components/edit-form.test.tsx</files>
  <read_first>
    - frontend/src/app/logs/_components/create-form.tsx (see form fields, validation, submission logic)
    - frontend/src/app/logs/_components/edit-form.tsx (see pre-population, update logic)
    - frontend/__tests__/utils/test-utils.tsx (see custom render function)
    - frontend/src/lib/api.ts (see createLog and updateLog function signatures)
  </read_first>
  <behavior>
    CreateForm tests:
    - Renders all form fields (message textarea, severity select, source input, timestamp input)
    - Shows validation error when message field is empty on submit
    - Shows validation error when source field is empty on submit
    - Calls createLog API function with correct data on valid submission
    - Calls onSuccess callback after successful API response
    - Disables submit button during API call (loading state)
    - Shows error message when API call fails
    - Has no accessibility violations (jest-axe)

    EditForm tests:
    - Pre-populates all fields with log data (message, severity, source, timestamp)
    - Shows validation errors for empty required fields
    - Calls updateLog API function with log id and updated data
    - Calls onSuccess callback after successful update
    - Calls onCancel callback when cancel button clicked
    - Disables buttons during API call (loading state)
    - Shows error message when API call fails
    - Has no accessibility violations (jest-axe)
  </behavior>
  <action>
Create frontend/__tests__/components/create-form.test.tsx:

```typescript
import { render, screen, waitFor } from '@/__tests__/utils/test-utils'
import userEvent from '@testing-library/user-event'
import { axe } from 'jest-axe'
import { CreateForm } from '@/app/logs/_components/create-form'
import * as api from '@/lib/api'

jest.mock('@/lib/api')

describe('CreateForm', () => {
  beforeEach(() => {
    jest.clearAllMocks()
  })

  it('renders all form fields', () => {
    render(<CreateForm />)
    expect(screen.getByLabelText(/message/i)).toBeInTheDocument()
    expect(screen.getByLabelText(/severity/i)).toBeInTheDocument()
    expect(screen.getByLabelText(/source/i)).toBeInTheDocument()
    expect(screen.getByLabelText(/timestamp/i)).toBeInTheDocument()
  })

  it('shows validation error for empty message', async () => {
    const user = userEvent.setup()
    render(<CreateForm />)

    await user.click(screen.getByRole('button', { name: /create/i }))

    expect(await screen.findByText(/message.*required/i)).toBeInTheDocument()
  })

  it('submits valid form data', async () => {
    const user = userEvent.setup()
    const mockOnSuccess = jest.fn()
    const mockCreateLog = jest.spyOn(api, 'createLog').mockResolvedValue({
      id: 1,
      timestamp: '2024-01-15T10:30:00Z',
      message: 'Test message',
      severity: 'INFO',
      source: 'test-source'
    })

    render(<CreateForm onSuccess={mockOnSuccess} />)

    await user.type(screen.getByLabelText(/message/i), 'Test message')
    await user.type(screen.getByLabelText(/source/i), 'test-source')
    await user.click(screen.getByRole('button', { name: /create/i }))

    await waitFor(() => {
      expect(mockCreateLog).toHaveBeenCalledWith(
        expect.objectContaining({
          message: 'Test message',
          source: 'test-source',
        })
      )
      expect(mockOnSuccess).toHaveBeenCalled()
    })
  })

  it('disables submit button during submission', async () => {
    const user = userEvent.setup()
    jest.spyOn(api, 'createLog').mockImplementation(() => new Promise(resolve => setTimeout(resolve, 100)))

    render(<CreateForm />)

    await user.type(screen.getByLabelText(/message/i), 'Test')
    await user.type(screen.getByLabelText(/source/i), 'test-source')

    const submitButton = screen.getByRole('button', { name: /create/i })
    await user.click(submitButton)

    expect(submitButton).toBeDisabled()
  })

  it('handles API error gracefully', async () => {
    const user = userEvent.setup()
    jest.spyOn(api, 'createLog').mockRejectedValue(new Error('API Error'))

    render(<CreateForm />)

    await user.type(screen.getByLabelText(/message/i), 'Test')
    await user.type(screen.getByLabelText(/source/i), 'test-source')
    await user.click(screen.getByRole('button', { name: /create/i }))

    await waitFor(() => {
      expect(screen.getByRole('button', { name: /create/i })).not.toBeDisabled()
    })
  })

  it('has no accessibility violations', async () => {
    const { container } = render(<CreateForm />)
    const results = await axe(container)
    expect(results).toHaveNoViolations()
  })
})
```

Create frontend/__tests__/components/edit-form.test.tsx:

```typescript
import { render, screen, waitFor } from '@/__tests__/utils/test-utils'
import userEvent from '@testing-library/user-event'
import { axe } from 'jest-axe'
import { EditForm } from '@/app/logs/_components/edit-form'
import * as api from '@/lib/api'

jest.mock('@/lib/api')

const mockLog = {
  id: 1,
  timestamp: '2024-01-15T10:30:00Z',
  message: 'Original message',
  severity: 'INFO' as const,
  source: 'original-source'
}

describe('EditForm', () => {
  beforeEach(() => {
    jest.clearAllMocks()
  })

  it('pre-populates fields with log data', () => {
    render(<EditForm log={mockLog} onSuccess={jest.fn()} onCancel={jest.fn()} />)

    expect(screen.getByLabelText(/message/i)).toHaveValue('Original message')
    expect(screen.getByLabelText(/severity/i)).toHaveValue('INFO')
    expect(screen.getByLabelText(/source/i)).toHaveValue('original-source')
  })

  it('submits updated data', async () => {
    const user = userEvent.setup()
    const mockOnSuccess = jest.fn()
    const mockUpdateLog = jest.spyOn(api, 'updateLog').mockResolvedValue({
      ...mockLog,
      message: 'Updated message'
    })

    render(<EditForm log={mockLog} onSuccess={mockOnSuccess} onCancel={jest.fn()} />)

    const messageInput = screen.getByLabelText(/message/i)
    await user.clear(messageInput)
    await user.type(messageInput, 'Updated message')
    await user.click(screen.getByRole('button', { name: /save|update/i }))

    await waitFor(() => {
      expect(mockUpdateLog).toHaveBeenCalledWith(
        1,
        expect.objectContaining({
          message: 'Updated message',
        })
      )
      expect(mockOnSuccess).toHaveBeenCalled()
    })
  })

  it('calls onCancel when cancel button clicked', async () => {
    const user = userEvent.setup()
    const mockOnCancel = jest.fn()

    render(<EditForm log={mockLog} onSuccess={jest.fn()} onCancel={mockOnCancel} />)

    await user.click(screen.getByRole('button', { name: /cancel/i }))

    expect(mockOnCancel).toHaveBeenCalled()
  })

  it('disables buttons during submission', async () => {
    const user = userEvent.setup()
    jest.spyOn(api, 'updateLog').mockImplementation(() => new Promise(resolve => setTimeout(resolve, 100)))

    render(<EditForm log={mockLog} onSuccess={jest.fn()} onCancel={jest.fn()} />)

    const saveButton = screen.getByRole('button', { name: /save|update/i })
    await user.click(saveButton)

    expect(saveButton).toBeDisabled()
  })

  it('has no accessibility violations', async () => {
    const { container } = render(<EditForm log={mockLog} onSuccess={jest.fn()} onCancel={jest.fn()} />)
    const results = await axe(container)
    expect(results).toHaveNoViolations()
  })
})
```

Import patterns:
- Use @/__tests__/utils/test-utils for render, screen
- Use @testing-library/user-event for realistic interactions
- Use jest-axe for accessibility testing
- Mock @/lib/api module with jest.mock()
  </action>
  <verify>
    <automated>
npm test -- --testPathPattern=create-form.test.tsx --passWithNoTests && \
npm test -- --testPathPattern=edit-form.test.tsx --passWithNoTests
    </automated>
  </verify>
  <acceptance_criteria>
    - frontend/__tests__/components/create-form.test.tsx exists with 6+ tests
    - frontend/__tests__/components/edit-form.test.tsx exists with 5+ tests
    - CreateForm tests cover: rendering, validation, submission, loading state, error handling, accessibility
    - EditForm tests cover: pre-population, update, cancel, loading state, accessibility
    - All tests use userEvent for interactions (not fireEvent)
    - All tests use accessibility-focused queries (getByLabelText, getByRole)
    - Both test files include jest-axe accessibility validation
    - All tests mock @/lib/api module
    - Tests use async/await with waitFor for asynchronous assertions
  </acceptance_criteria>
  <done>CreateForm and EditForm have comprehensive tests covering user interactions, form validation, API calls, loading states, error handling, and accessibility</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create modal and API integration tests</name>
  <files>frontend/__tests__/components/log-detail-modal.test.tsx, frontend/__tests__/api/api-integration.test.tsx</files>
  <read_first>
    - frontend/src/app/logs/_components/log-detail-modal.tsx (see view/edit mode toggle, delete confirmation)
    - frontend/src/lib/api.ts (see all exported functions: fetchLogs, fetchLogById, createLog, updateLog, deleteLog, exportLogs)
  </read_first>
  <behavior>
    LogDetailModal tests:
    - Renders log details in view mode (message, severity, source, timestamp)
    - Shows Edit and Delete buttons in view mode
    - Switches to edit mode when Edit button clicked
    - Shows EditForm component in edit mode
    - Calls onClose when modal closed
    - Does not render when isOpen is false
    - Has no accessibility violations

    API integration tests:
    - fetchLogs returns LogListResponse with logs array and pagination
    - fetchLogById returns single LogResponse
    - createLog sends POST with LogCreate data
    - updateLog sends PUT with id and LogCreate data
    - deleteLog sends DELETE with id
    - exportLogs returns Blob for CSV download
    - All functions handle API errors gracefully (throw or return error)
  </behavior>
  <action>
Create frontend/__tests__/components/log-detail-modal.test.tsx:

```typescript
import { render, screen } from '@/__tests__/utils/test-utils'
import userEvent from '@testing-library/user-event'
import { axe } from 'jest-axe'
import { LogDetailModal } from '@/app/logs/_components/log-detail-modal'

const mockLog = {
  id: 1,
  timestamp: '2024-01-15T10:30:00Z',
  message: 'Test log message',
  severity: 'INFO' as const,
  source: 'test-service'
}

describe('LogDetailModal', () => {
  it('renders log details in view mode', () => {
    render(<LogDetailModal log={mockLog} isOpen={true} onClose={jest.fn()} />)

    expect(screen.getByText('Test log message')).toBeInTheDocument()
    expect(screen.getByText(/INFO/i)).toBeInTheDocument()
    expect(screen.getByText('test-service')).toBeInTheDocument()
  })

  it('shows Edit and Delete buttons in view mode', () => {
    render(<LogDetailModal log={mockLog} isOpen={true} onClose={jest.fn()} />)

    expect(screen.getByRole('button', { name: /edit/i })).toBeInTheDocument()
    expect(screen.getByRole('button', { name: /delete/i })).toBeInTheDocument()
  })

  it('switches to edit mode when Edit button clicked', async () => {
    const user = userEvent.setup()
    render(<LogDetailModal log={mockLog} isOpen={true} onClose={jest.fn()} />)

    await user.click(screen.getByRole('button', { name: /edit/i }))

    // Edit form should appear with pre-filled message field
    expect(screen.getByLabelText(/message/i)).toHaveValue('Test log message')
  })

  it('does not render when isOpen is false', () => {
    render(<LogDetailModal log={mockLog} isOpen={false} onClose={jest.fn()} />)

    expect(screen.queryByText('Test log message')).not.toBeInTheDocument()
  })

  it('calls onClose when modal closed', async () => {
    const user = userEvent.setup()
    const mockOnClose = jest.fn()
    render(<LogDetailModal log={mockLog} isOpen={true} onClose={mockOnClose} />)

    // Close button or Escape key (implementation dependent)
    const closeButton = screen.getByRole('button', { name: /close/i })
    await user.click(closeButton)

    expect(mockOnClose).toHaveBeenCalled()
  })

  it('has no accessibility violations', async () => {
    const { container } = render(<LogDetailModal log={mockLog} isOpen={true} onClose={jest.fn()} />)
    const results = await axe(container)
    expect(results).toHaveNoViolations()
  })
})
```

Create frontend/__tests__/api/api-integration.test.tsx:

```typescript
import {
  fetchLogs,
  fetchLogById,
  createLog,
  updateLog,
  deleteLog,
  exportLogs,
} from '@/lib/api'

// Mock global fetch
global.fetch = jest.fn()

describe('API Integration', () => {
  beforeEach(() => {
    jest.clearAllMocks()
    ;(global.fetch as jest.Mock).mockResolvedValue({
      ok: true,
      json: async () => ({}),
    })
  })

  describe('fetchLogs', () => {
    it('returns paginated logs', async () => {
      const mockResponse = {
        logs: [
          { id: 1, timestamp: '2024-01-15T10:30:00Z', message: 'Log 1', severity: 'INFO', source: 'service-1' },
          { id: 2, timestamp: '2024-01-15T11:30:00Z', message: 'Log 2', severity: 'ERROR', source: 'service-2' },
        ],
        next_cursor: 'cursor123',
        has_more: true,
      }
      ;(global.fetch as jest.Mock).mockResolvedValueOnce({
        ok: true,
        json: async () => mockResponse,
      })

      const result = await fetchLogs({})

      expect(result.logs).toHaveLength(2)
      expect(result.has_more).toBe(true)
      expect(global.fetch).toHaveBeenCalledWith(expect.stringContaining('/api/logs'))
    })

    it('includes filter parameters in query string', async () => {
      await fetchLogs({ severity: ['ERROR'], source: 'api-service' })

      expect(global.fetch).toHaveBeenCalledWith(
        expect.stringContaining('severity=ERROR')
      )
      expect(global.fetch).toHaveBeenCalledWith(
        expect.stringContaining('source=api-service')
      )
    })
  })

  describe('fetchLogById', () => {
    it('returns single log', async () => {
      const mockLog = { id: 1, timestamp: '2024-01-15T10:30:00Z', message: 'Log 1', severity: 'INFO', source: 'service-1' }
      ;(global.fetch as jest.Mock).mockResolvedValueOnce({
        ok: true,
        json: async () => mockLog,
      })

      const result = await fetchLogById(1)

      expect(result.id).toBe(1)
      expect(global.fetch).toHaveBeenCalledWith(expect.stringContaining('/api/logs/1'))
    })
  })

  describe('createLog', () => {
    it('sends POST with log data', async () => {
      const newLog = {
        timestamp: '2024-01-15T10:30:00Z',
        message: 'New log',
        severity: 'INFO' as const,
        source: 'test-service',
      }
      ;(global.fetch as jest.Mock).mockResolvedValueOnce({
        ok: true,
        json: async () => ({ id: 1, ...newLog }),
      })

      await createLog(newLog)

      expect(global.fetch).toHaveBeenCalledWith(
        expect.stringContaining('/api/logs'),
        expect.objectContaining({
          method: 'POST',
          body: JSON.stringify(newLog),
        })
      )
    })
  })

  describe('updateLog', () => {
    it('sends PUT with id and updated data', async () => {
      const updatedLog = {
        timestamp: '2024-01-15T10:30:00Z',
        message: 'Updated log',
        severity: 'ERROR' as const,
        source: 'test-service',
      }
      ;(global.fetch as jest.Mock).mockResolvedValueOnce({
        ok: true,
        json: async () => ({ id: 1, ...updatedLog }),
      })

      await updateLog(1, updatedLog)

      expect(global.fetch).toHaveBeenCalledWith(
        expect.stringContaining('/api/logs/1'),
        expect.objectContaining({
          method: 'PUT',
          body: JSON.stringify(updatedLog),
        })
      )
    })
  })

  describe('deleteLog', () => {
    it('sends DELETE with id', async () => {
      ;(global.fetch as jest.Mock).mockResolvedValueOnce({
        ok: true,
      })

      await deleteLog(1)

      expect(global.fetch).toHaveBeenCalledWith(
        expect.stringContaining('/api/logs/1'),
        expect.objectContaining({
          method: 'DELETE',
        })
      )
    })
  })

  describe('exportLogs', () => {
    it('returns Blob for CSV download', async () => {
      const mockBlob = new Blob(['csv content'], { type: 'text/csv' })
      ;(global.fetch as jest.Mock).mockResolvedValueOnce({
        ok: true,
        blob: async () => mockBlob,
      })

      const result = await exportLogs({})

      expect(result).toBeInstanceOf(Blob)
      expect(global.fetch).toHaveBeenCalledWith(expect.stringContaining('/api/export'))
    })
  })

  describe('error handling', () => {
    it('throws error when fetch fails', async () => {
      ;(global.fetch as jest.Mock).mockResolvedValueOnce({
        ok: false,
        status: 500,
      })

      await expect(fetchLogs({})).rejects.toThrow()
    })
  })
})
```

Pattern:
- Mock global.fetch for API calls
- Test request parameters (method, body, query string)
- Test response parsing (json, blob)
- Test error handling (ok: false)
  </action>
  <verify>
    <automated>
npm test -- --testPathPattern=log-detail-modal.test.tsx --passWithNoTests && \
npm test -- --testPathPattern=api-integration.test.tsx --passWithNoTests
    </automated>
  </verify>
  <acceptance_criteria>
    - frontend/__tests__/components/log-detail-modal.test.tsx exists with 6+ tests
    - frontend/__tests__/api/api-integration.test.tsx exists with 8+ tests covering all API functions
    - LogDetailModal tests cover: rendering, mode toggle, close callback, visibility, accessibility
    - API tests cover: fetchLogs, fetchLogById, createLog, updateLog, deleteLog, exportLogs, error handling
    - API tests verify HTTP method, URL, request body, and response parsing
    - API tests use global.fetch mock (not MSW)
    - All tests use descriptive test names and clear assertions
    - Tests follow existing patterns from __tests__/smoke.test.tsx
  </acceptance_criteria>
  <done>LogDetailModal and API client functions have comprehensive tests covering user interactions, API request/response handling, and error cases</done>
</task>

<task type="auto">
  <name>Task 4: Run frontend tests with coverage and verify 80%+ target</name>
  <files>None (verification task)</files>
  <read_first>
    - frontend/jest.config.js (confirm coverage configuration)
  </read_first>
  <action>
Execute frontend test suite with coverage measurement:
1. Run tests with coverage: npm test -- --coverage --passWithNoTests
2. Verify coverage reports generated:
   - Terminal output shows coverage summary
   - frontend/coverage/lcov-report/index.html exists for detailed drill-down
3. Check overall line coverage percentage in terminal output
4. If coverage < 80%, identify gaps from coverage report and add targeted tests

For coverage gaps found:
- Add tests for any untested form fields or validation logic
- Add tests for API error cases not covered
- Add tests for modal state transitions
- Add tests for filter components or other interactive elements

Target: 80%+ line coverage for src/ directory (excluding src/components/ui/** per collectCoverageFrom configuration)

Why run coverage now: Validates frontend achieves TEST-01 requirement, ensures critical paths are tested
  </action>
  <verify>
    <automated>
npm test -- --coverage --passWithNoTests 2>&1 | grep -E "All files.*[8-9][0-9]\.[0-9]+|All files.*100"
    </automated>
  </verify>
  <acceptance_criteria>
    - Command "npm test -- --coverage --passWithNoTests" exits with code 0
    - Terminal output shows coverage summary with "All files" line >= 80%
    - File frontend/coverage/lcov-report/index.html exists
    - Coverage report excludes src/components/ui/** per collectCoverageFrom configuration
    - All tests pass with no failures
    - Test output shows 20+ tests passing across 4 new test files
  </acceptance_criteria>
  <done>Frontend test suite achieves 80%+ line coverage with Jest, all critical paths tested (forms, modals, API integration), coverage reports generated</done>
</task>

</tasks>

<verification>
Run full frontend test suite with coverage:
```bash
cd frontend && npm test -- --coverage --passWithNoTests
```

Expected output:
- All tests pass (20+ tests)
- Coverage report shows >= 80% line coverage for src/
- frontend/coverage/lcov-report/index.html generated

Verify specific files:
- frontend/package.json contains jest-axe
- frontend/jest.config.js contains coverageThreshold configuration
- frontend/__tests__/components/create-form.test.tsx exists with 6+ tests
- frontend/__tests__/components/edit-form.test.tsx exists with 5+ tests
- frontend/__tests__/components/log-detail-modal.test.tsx exists with 6+ tests
- frontend/__tests__/api/api-integration.test.tsx exists with 8+ tests
</verification>

<success_criteria>
1. jest-axe installed and configured in frontend/package.json and __tests__/setup.ts
2. frontend/jest.config.js configured with 80% coverage threshold and exclusions
3. Frontend test suite achieves 80%+ line coverage measured by Jest
4. CreateForm and EditForm have comprehensive tests (11+ tests total)
5. LogDetailModal has tests covering view/edit modes and interactions
6. API client functions have integration tests with mocked fetch (8+ tests)
7. All tests include accessibility validation with jest-axe
8. Coverage reports generate terminal summary and HTML output
9. All tests pass consistently with no failures
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing/06-02-SUMMARY.md`
</output>
